import { Box } from "@mui/material";
import React from "react";

export const Sidebar = () => {
  return (
    <Box
      sx={{
        background: "red",
        p: 2,
      }}
      flex={1}
    >
      Sidebar
    </Box>
  );
};
